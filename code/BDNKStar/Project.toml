name = "BDNKStar"
uuid = "bd9c0a1e-2f73-4d6a-9e84-1c5b7a2f3d6e"
authors = ["BDNK-HMNS project"]
version = "0.1.0"

# Core physics package is intentionally STDLIB-ONLY so that the round-trip /
# causality test gate instantiates and runs fast (< 20 threads, no GPU).
# Figure generation lives in a separate environment (viz/) to keep this lean.

[deps]
LinearAlgebra = "37e2e46d-f89d-539d-b4ee-838fcccc9c8e"
Printf = "de0858da-6303-5e67-8744-51eddeeeb8d7"

[compat]
julia = "1.10"

[extras]
Test = "8dfed614-e22c-5e08-85e1-65c5234f0b40"

[targets]
test = ["Test"]
